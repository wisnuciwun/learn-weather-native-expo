import { Text } from 'react-native';
import { StatusBar } from 'expo-status-bar';

export default function Welcome() {
     return (
          <>
               <Text>Hi all ! This is my first react native app with expo</Text>
               <StatusBar style="auto" />
          </>
     );
}
